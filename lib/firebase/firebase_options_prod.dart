import 'package:firebase_core/firebase_core.dart';

/// Placeholder for the FlutterFire-generated options file (production).
///
/// Run `flutterfire configure` for your production Firebase project and
/// replace this file with the generated `firebase_options.dart` content
/// (keep the file name `firebase_options_prod.dart`), then set
/// `useFirebase = true` in main.dart. See CHECKLIST.md.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured. Run `flutterfire configure` and replace '
      'lib/firebase/firebase_options_prod.dart with the generated options.',
    );
  }
}
